# Production Requirements (Exact versions from working local environment)
# Core Web Framework
flask==3.1.2
flask-cors==6.0.1
gunicorn==23.0.0
werkzeug==3.1.3

# HTTP & Web Scraping
requests==2.32.5
beautifulsoup4==4.14.2
lxml==6.0.2

# NLP & Text Processing (Lightweight)
nltk==3.9.2

# AI & LLM Framework
langchain==1.0.5
langchain-core==1.0.4
langchain-text-splitters==1.0.0
langchain_community==0.4.1
langchain-google-genai==3.0.1
langchain_experimental==0.0.42
langchain-tavily==0.2.13

# Vector Database
pinecone==7.3.0
langchain-pinecone==0.2.13

# Document Processing
pypdf==6.1.3

# Configuration & Environment
python-dotenv==1.0.1

# Image Processing (Lightweight)
Pillow==12.0.0

# Translation & TTS
deep-translator==1.11.4
gTTS==2.5.4
edge-tts==7.2.3

# Web Search
tavily-python==0.7.12

# HuggingFace (Without heavy models)
huggingface_hub==0.36.0

# Google AI
google-ai-generativelanguage==0.9.0
google-api-core==2.28.1
google-auth==2.43.0

# REMOVED FOR CLOUD DEPLOYMENT:
# - pyaudio (requires audio drivers, not available in cloud)
# - pyttsx3 (requires system TTS engines)
# - spacy (large models, high memory usage)
# - sentence-transformers (heavy, using API embeddings instead)
# - SpeechRecognition (handled client-side via browser)
# - pydub (audio processing not needed server-side)
# Added for HuggingFace local embeddings
# NOTE: heavy ML packages removed to avoid large on-instance installs (use remote embeddings)
# If you require local embeddings on the server, re-add:
# sentence-transformers==2.2.2
# torch==2.2.0

# Note: If you need embeddings, use API-based solutions like:
# - OpenAI embeddings
# - Cohere embeddings
# - Google embeddings (via langchain-google-genai)
